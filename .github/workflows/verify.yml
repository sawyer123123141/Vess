name: Verify Vess

on:
  push:
  pull_request:

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-ci.txt

      - name: Install CI dependencies
        run: python -m pip install -r requirements-ci.txt

      - name: Run unit tests
        run: python -m unittest discover -s tests -v

  behavior-preview:
    needs: unit-tests
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install preview dependencies
        run: python -m pip install numpy pillow

      - name: Run behavior verification
        id: behavior
        shell: bash
        run: |
          set +e
          python tools/render_behavior_preview.py
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Run comprehensive eye validation
        id: eye_validation
        shell: bash
        run: |
          set +e
          python tools/render_eye_validation.py
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Add behavior summaries
        if: always()
        shell: bash
        run: |
          if [ -f artifacts/behavior-verification/summary.txt ]; then
            cat artifacts/behavior-verification/summary.txt >> "$GITHUB_STEP_SUMMARY"
          else
            echo "Behavior verification produced no summary." >> "$GITHUB_STEP_SUMMARY"
          fi
          echo "" >> "$GITHUB_STEP_SUMMARY"
          if [ -f artifacts/behavior-verification/eye_validation_summary.txt ]; then
            cat artifacts/behavior-verification/eye_validation_summary.txt >> "$GITHUB_STEP_SUMMARY"
          else
            echo "Eye validation produced no summary." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload behavior artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vess-behavior-verification
          path: artifacts/behavior-verification/
          if-no-files-found: warn

      - name: Fail on verification error
        if: always()
        shell: bash
        run: |
          behavior_code="${{ steps.behavior.outputs.exit_code }}"
          eye_code="${{ steps.eye_validation.outputs.exit_code }}"
          if [ -z "$behavior_code" ]; then
            behavior_code=2
          fi
          if [ -z "$eye_code" ]; then
            eye_code=2
          fi
          if [ "$behavior_code" -ne 0 ]; then
            exit "$behavior_code"
          fi
          exit "$eye_code"
